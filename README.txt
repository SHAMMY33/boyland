Files in this folder:
- home_cleaned.html : cleaned local HTML copy with scripts removed
- inline-extracted.css : all inline <style> blocks extracted from home.txt
- external-stylesheets.txt : external stylesheet URLs still referenced by the page

Note: this cleaned copy still references the live Boylan CDN CSS files.
If you want a fully offline clone, download each URL in external-stylesheets.txt
into this same folder and replace the href values with local filenames.
